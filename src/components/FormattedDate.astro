---
interface Props {
  date: Date;
}

const { date } = Astro.props;
const date_ = new Date(date);
---

<time datetime={date_.toISOString()}>
  {
    date_.toLocaleDateString('en-us', {
      year: 'numeric',
      month: 'short',
      day: 'numeric'
    })
  }
</time>
